abs = 절댓값
# print(abs(-5)) = 5

pow(4,2) = 4^2

max = 최댓값을 골라줌
print(max(5,12)) = 12

min = 최소값

round = 반올림

from () import *
=여러가지 연산자를 쓸 수 있게 해줌
(아직 잘 모름)

from random import *
random() 1 미만의 값을 아무거나 뽑아줌 * 숫자 붙이면 그 숫자 까지의 임의의 값 생성
random()+ 그 숫자 부터
int로 감싸면 정수만
randrange(x, y) x ~ y 미만의 값 랜덤 생성
randint(x, y) xy도 범위에 포함

<!--숫자열-->

슬라이싱: 필요한 부분만 잘라서 사용하는 것.

ex: 변수명[숫자] = 해당 영역의 문자를 가져옴.
ex: x = '4321' <- 0부터 시작
    print(x[3]) = 출력: 1
    [x:y] x 부터 y직전(y가 포함x)까지
    y를 비워두면 x부터 끝까지
    -x 뒤에서 x번째 부터

변수.upper 대문자로
변수.lower 소문자로 
len(변수) 문자열 길이   
변수.replace(변수의 문자열, 바꿀 문자) 문자열 값을 바꿈

index = 변수명.index("문자")
print(index) 문자의 위치를 출력  #엑셀 느낌
+ 1 등의 변형도 가능

비슷한 함수로는 find가 있음
단, find는 찾는 글자가ㅜ 없으면 -1을 출력해줌
index는 에러가 나서 프로그램이 종료 됨

count("x") x가 몇번 등장하는지 세어줌

문자열 포맷
나는 %d 살입니다. %20 # 뒤에 % 값을 대입 d는 정수 
%s %"str" 뒤에 문자열을 대입 s는 문자열
%c %"A" c는 한글자만 

%s색 %색 %(파란, 빨간) 두가지 각각 순차 대입
식으로 변형 가능 

나는 {}살입니다 .format(20) <- 대입 가능
순차 대입 가능

중괄호에 숫자를 넣어서 포맷()의 몇번째 식으로 선택 가능.
변수 대입도 가능

탈출문자

\n 줄 바꿈  
\" "를 단순 문자로 출력하게 해줌
\\ 는 \ 를 표현
\r 커서를 맨 앞으로 이동
ex: print("red apple\rpine) == pineapple
\b 백스페이스(한 글자 지움)
\t 탭 (여러 칸 공백)

<!--문자열-->

